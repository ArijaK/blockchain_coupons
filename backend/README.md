# Backend

Necessary dependencies can be installed by running:

    npm install

Contains simple TypeScript scripts to check connectivity (without running backend service). To execute them, call:

    npx ts-node ./test/test_example.ts

To run backend service, build it and run it using:
    npm run build
    npm run start