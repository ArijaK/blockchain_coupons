# Backend

Necessary dependencies can be installed by running:

    npm install

Currently only contains simple TypeScript scripts to check connectivity. To execute them, call:

    npx ts-node ./test/test_example.ts